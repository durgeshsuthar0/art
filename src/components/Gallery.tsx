import { useMemo, useState } from 'react'; import { artworks,categories } from '../data/artworks'; import Lightbox from './Lightbox';
export default function Gallery(){const [cat,setCat]=useState('All'); const [idx,setIdx]=useState<number|null>(null); const visible=useMemo(()=>cat==='All'?artworks:artworks.filter(a=>a.category===cat),[cat]); return <section id="works" className="works section"><div className="works-head"><div><p className="section-index">02 / Selected works</p><h2>Artwork across walls, canvas, illustration and decorative expression.</h2></div><div className="filters">{categories.map(c=><button className={cat===c?'active':''} onClick={()=>setCat(c)} key={c}>{c}</button>)}</div></div><div className="masonry">{visible.map((art,i)=><button className={'art-card '+(art.orientation==='landscape'?'wide':'')} key={art.id} onClick={()=>setIdx(i)}><div className="image-wrap"><img loading="lazy" src={art.image} alt={art.title}/></div><div className="caption"><span>{art.category}</span><h3>{art.title}</h3></div></button>)}</div>{idx!==null&&<Lightbox items={visible} index={idx} onIndex={setIdx} onClose={()=>setIdx(null)}/>}</section>}