import { X, ChevronLeft, ChevronRight } from 'lucide-react'; import type { Artwork } from '../data/artworks';
export default function Lightbox({items,index,onClose,onIndex}:{items:Artwork[];index:number;onClose:()=>void;onIndex:(n:number)=>void}){const art=items[index]; const go=(d:number)=>onIndex((index+d+items.length)%items.length); return <div className="lightbox" role="dialog" aria-modal="true"><button className="lb-close" onClick={onClose}><X/></button><button className="lb-nav left" onClick={()=>go(-1)}><ChevronLeft/></button><div className="lb-content"><img src={art.image} alt={art.title}/><div><p>{art.category}</p><h3>{art.title}</h3><span>{art.description}</span></div></div><button className="lb-nav right" onClick={()=>go(1)}><ChevronRight/></button></div>}